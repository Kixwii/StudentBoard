# Web Core Module

**Purpose**: Phoenix web infrastructure — Endpoint, Router, Telemetry, ErrorJSON, Gettext.

**Naming**: snake_case.ex, modules under `SchoolPortalApiWeb.*` (Phoenix convention — no Core. prefix on Endpoint/Router)
